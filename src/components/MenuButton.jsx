import React from 'react'

const MenuButton = ({color="blue", logo, title}) => {
  return (
    <div>
        logo
        <div>
            <p>
                {title}
            </p>
        </div>
    </div>
  )
}

export default MenuButton